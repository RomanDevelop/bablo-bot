# bablo-bot

Bablo Trading Service — Flutter-кабинет для MACD trading bot (Binance).

## Run

```bash
fvm flutter pub get
fvm flutter run -d chrome
```

API (default): `http://18.197.147.209:8000/api/v1`
