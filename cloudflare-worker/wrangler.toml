name = "bablo-bot-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Free Cloudflare Workers plan — no Firebase Blaze required.
# After deploy URL: https://bablo-bot-api.<your-subdomain>.workers.dev
