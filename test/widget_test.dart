import 'package:flutter_test/flutter_test.dart';
import 'package:crypto_trading_bot/main.dart';

void main() {
  testWidgets('Bablo app boots', (tester) async {
    // Smoke test placeholder — full widget tests need mocked repository.
    expect(BabloApp, isNotNull);
  });
}
